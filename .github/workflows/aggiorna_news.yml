# ... dentro lo script python del file .yml ...

# Prende i segreti dalle "Environment Variables" di GitHub
caveau_dati = os.environ.get('DATI_PRIVATI', 'Nessun dato salvato.')
pass_segreta = os.environ.get('PASSWORD_SITO', 'Delfino2026')
token_telecomando = os.environ.get('GH_TOKEN', '')

# Crea il file che il sito leggerà
pacchetto = {
    "notizie": notizie, # Le news generate da Gemini
    "caveau": caveau_dati,
    "chiave_accesso": pass_segreta,
    "telecomando": token_telecomando 
}

# Salva tutto nel file notizie.json
with open('notizie.json', 'w') as f:
    json.dump(pacchetto, f, indent=4)
